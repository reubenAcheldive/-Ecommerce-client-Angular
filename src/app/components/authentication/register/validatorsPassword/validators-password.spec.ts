import { ValidatorsPassword } from './validators-password';

describe('ValidatorsPassword', () => {
  it('should create an instance', () => {
    expect(new ValidatorsPassword()).toBeTruthy();
  });
});
