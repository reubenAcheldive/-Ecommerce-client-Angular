import { ProductStyleDirective } from './product-style.directive';

describe('ProductStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductStyleDirective();
    expect(directive).toBeTruthy();
  });
});
